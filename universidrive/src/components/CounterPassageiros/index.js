import React, { Component } from 'react';
import Button from "../../components/Button";

const CounterPassageiros = ({}) => {
    return(
        function NumPassageiros() {
            
        }
    )
}